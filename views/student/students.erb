	<div class="container">	
		<div class="row">  
			<div class="col-md-12">
				<a href="/admin/admin_edit" class="btn btn-info btn-small" style="margin-top: 12px; float: right;">Admin Index</a>
				<h1 class="page-header">Student List</h1>
			</div>
		</div>
		
		<div class="row">
			<div class="col-md-6">
				<form class="form-search form-inline" method="get">
					<div class="input-group">
		
						<input type="text" class="search-query form-control" name="search" placeholder="Search Email" aria-label="Search email" aria-describedby="Search Email" value="<%= params[:search] %>">
						<button class="btn btn-info" type="submit" >Find</button>
		 
					</div>
				</form>
			</div>
		</div>
		
		
		
		
		
		
		
		<!-- <div class="row">
			<div class="col-md-12" style="margin-bottom: 1em;">
				<p class="pull-right">Total number of students: <strong><%= @student.count %></strong></p>
				<form class="form-search" method="get">
				<div class="input-append">
					<input type="text" class="textfield col-md-6 search-query" style="padding: 5px; margin-right: 12px;" name="search" placeholder="Search Email" value="<%= params[:search] %>" />
					<button type="submit" class="btn btn-success">Find</button>
				</div>
				</form>
			</div>
		</div> -->
		<div class="row"> 
			<div class="col-md-12">
				
			<table class="table table-striped table-sm table-hover" style="margin-top: 1em;">
					<tr class="table-success">
						<th>Name</th>
						<th>Email</th>
						<th>School ID</th>
						
					</tr>
					<% @student.sort_by(&:updated_at).reverse.each do |student| %>
					<tr>
						<td><%= student.first_name %> <%= student.last_name %></td>
						<td><%= student.email %></td>
						<td><%= student.school_password %></td>
						
					</tr>
					<% end %>
					<tr>
						<th colspan="3"><small>Total number of students: <strong><%= @student.count %></strong></small>
						</th>
					</tr>
				</table>
				
			</div>
			
		</div> <!-- /row -->
	</div> <!-- /container -->
<script type="text/javascript">
	$(function() {
	  $(document).ready(function() {
	    $('a.delete').click(function() {
	      if (!confirm('This cannot be undone! Continue?')) {
	        return false;
	      }
	    });
	  });
	}).call(this);
</script>
	
 
 
 
